# =============================================================================
# Creative-lovable Environment Configuration
# =============================================================================
# Copy this file to .env.local and fill in your actual values
# At minimum, you need either AI Gateway OR direct provider API keys
# =============================================================================

# =============================================================================
# AI Gateway (Recommended - Primary)
# =============================================================================
# AI Gateway provides unified access to multiple providers with fallback support
# AI SDK createGateway() reads AI_GATEWAY_API_KEY
AI_GATEWAY_API_KEY=your_gateway_api_key_here
# Optional: override the gateway URL if you use a non-default endpoint
# AI_GATEWAY_URL=https://gateway.ai.cloudflare.com/v1/{account_id}/{gateway_id}
# Optional on Vercel runtime: VERCEL_OIDC_TOKEN can be used instead of API key

# =============================================================================
# Direct Provider API Keys (Fallback)
# =============================================================================
# These are used when AI Gateway is unavailable or as fallback
# You need at least ONE provider configured

# Anthropic (Claude models)
# Get from: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-api03-your_key_here

# Google (Gemini models)
# Get from: https://aistudio.google.com/app/apikey
GOOGLE_GENERATIVE_AI_API_KEY=your_google_key_here

# OpenAI (GPT models)
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-your_key_here

# OpenRouter (backup provider path when AI Gateway errors)
# Get from: https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-your_key_here
# Optional: defaults to https://openrouter.ai/api/v1
# OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
# Optional headers OpenRouter recommends for ranking/analytics
# OPENROUTER_HTTP_REFERER=https://your-app-domain.com
# OPENROUTER_APP_NAME=Creative-lovable

# =============================================================================
# Database (Required)
# =============================================================================
# PostgreSQL connection string
# Format: postgresql://user:password@host:port/database
# For local development with Docker: postgresql://postgres:postgres@localhost:5432/creative_lovable
# For Neon: postgresql://user:password@host.neon.tech/database?sslmode=require
DATABASE_URL=postgresql://user:password@localhost:5432/creative_lovable

# =============================================================================
# Supabase (Required for Auth & Storage)
# =============================================================================
# Get from: https://app.supabase.com/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# =============================================================================
# E2B (Required for Code Execution)
# =============================================================================
# Get from: https://e2b.dev/dashboard?tab=keys
E2B_API_KEY=e2b_your_key_here

# Optional: Custom E2B template (recommended for faster sandbox startup)
# After building your custom template (see README.md), set this to your template ID/name
E2B_TEMPLATE=your_template_id_here
# Backward compatible alias:
# E2B_TEMPLATE_ID=your_template_id_here

# =============================================================================
# API Authentication (Optional but Recommended)
# =============================================================================
# Used for securing API routes. If not set, API will be open (development only)
API_KEY=your_secure_random_api_key_here

# =============================================================================
# Rate Limiting (Optional)
# =============================================================================
# Upstash Redis for rate limiting (recommended for production)
# Get from: https://console.upstash.com/redis
UPSTASH_REDIS_REST_URL=https://your-url.upstash.io
UPSTASH_REDIS_REST_TOKEN=your_token_here

# =============================================================================
# Monitoring & Observability (Optional)
# =============================================================================
# Sentry for error tracking (optional)
# NEXT_PUBLIC_SENTRY_DSN=https://your-sentry-dsn

# =============================================================================
# Development Settings
# =============================================================================
# Node environment (development, production, test)
NODE_ENV=development

# Next.js dev server port (optional)
# PORT=3000

# Enable debug logging (optional, development only)
# DEBUG=true
